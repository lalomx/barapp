import { InputNumericDirective } from './input-numeric.directive';

describe('InputNumericDirective', () => {
  it('should create an instance', () => {
    const directive = new InputNumericDirective();
    expect(directive).toBeTruthy();
  });
});
