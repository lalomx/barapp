import { FormMetadataInput } from '../../../core/interfaces/formMetadataInput';
import { DropdownOptions } from './dropDownOptions';

export interface ModifyInputEventArgs {
    input: FormMetadataInput;
}
